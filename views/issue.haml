%a{ href: @model.get('html_url'), target: '_blank' }
  = @model.get('title')
  
  .meta
    .repo-name = @model.get('repository').name
    %abbr.timeago{ title: @model.get("created_at") }
      = @model.get("created_at")